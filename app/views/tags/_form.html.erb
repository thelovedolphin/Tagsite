<%= render "websites/links" %>

<%= form_with model: @tag do |form| %>
  <div>
    <%= form.label :title, "Tag Title" %><br>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.submit "Add Tag" %>
  </div>
<% end %>